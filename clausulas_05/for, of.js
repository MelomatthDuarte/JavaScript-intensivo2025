//Iteracion sobre arreglos:
let colores = ["rojo", "verde", "azul"];

for (let color of colores) {
    console.log(color);
}

//Iteracion sobre los caracteres de un string:
let saludo = "Buenos dias";

for (let caracter of saludo) {
    console.log(`El caracter actual es '${caracter}'`);
}